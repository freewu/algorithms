# 2582 · Moving documents
# Description
# In this topic, try to use the mv command to practice moving a specified file to a specified folder 
# by completing the following 3 steps in order.

# Step 1 move a file called code.txt to the directory temp.
mv code.txt temp

# Step 2 move the file called code_1.txt to the directory /nobody/temp/sub_dir_1
mv code_1.txt /nobody/temp/sub_dir_1

# Step 3 move the file named code.txt from the /nobody/temp directory to the /nobody/sub_dir directory
mv /nobody/temp/code.txt /nobody/sub_dir
