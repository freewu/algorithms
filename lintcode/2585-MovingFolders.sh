# 2585 · Moving folders
# Description
# In this topic, try to use the mv command 
# to move the specified directory and all its subdirectories and files to another directory 
# by completing the following 3 steps in order.

# Step 1 move a folder called target and move all its contents to another folder des.
mv target des

# Step 2 move the /nobody/sub_dir_1/sub_dir directory and its files, subdirectories and all the files in the subdirectories to the /nobody/sub_dir_2 directory
mv /nobody/sub_dir_1/sub_dir /nobody/sub_dir_2

# Step 3 move the /nobody/sub_dir_1/temp directory and all files in its files, subdirectories and subdirectories to the /nobody/ directory
mv /nobody/sub_dir_1/temp /nobody
